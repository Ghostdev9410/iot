*   The listener runs in a separate thread. If Odoo is restarted, the listener must be started manually (or an automated cron job could be added).
*   Currently supports basic authentication and SSL/TLS.
